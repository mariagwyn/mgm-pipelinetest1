import { ReplaceabletextPipe } from './replaceabletext.pipe';

describe('ReplaceabletextPipe', () => {
  it('create an instance', () => {
    const pipe = new ReplaceabletextPipe();
    expect(pipe).toBeTruthy();
  });
});
